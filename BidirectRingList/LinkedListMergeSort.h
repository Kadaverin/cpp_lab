#include "pch.h"


using namespace std;

template<typename Node>
void mergeSort(Node**);

template<typename Node>
void splitList(Node*, Node**, Node**);

template<typename Node>
Node* mergeList(Node*, Node*);




